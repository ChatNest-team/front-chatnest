<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".Messagerie">

    <!-- Profil Agent -->

    <ImageView
        android:id="@+id/ivAgentPhoto"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginTop="44dp"
        android:background="@drawable/circle_background"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_agent_default"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvNomAgent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nom de l’agent"
        android:textSize="18sp"
        android:textStyle="italic"
        app:layout_constraintTop_toTopOf="@id/ivAgentPhoto"
        app:layout_constraintStart_toEndOf="@id/ivAgentPhoto"
        android:layout_marginStart="20dp" />

    <TextView
        android:id="@+id/tvStatut"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Spécialisation"
        android:textColor="#4CAF50"
        android:textStyle="italic"
        app:layout_constraintTop_toBottomOf="@id/tvNomAgent"
        app:layout_constraintStart_toStartOf="@id/tvNomAgent"
        android:layout_marginTop="4dp" />

    <!-- Conteneur de messages (placeholder ici) -->
    <ScrollView
        android:id="@+id/messageScrollView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/ivAgentPhoto"
        app:layout_constraintBottom_toTopOf="@+id/messageContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/messageList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <!-- Formulaire de message collé en bas -->
    <LinearLayout
        android:id="@+id/messageContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="#EEEEEE"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <EditText
            android:id="@+id/etMessage"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:hint="Écrire un message..."
            android:minHeight="48dp"
            android:background="@drawable/message_input_background"
            android:padding="10dp" />

        <Button
            android:id="@+id/btnEnvoyer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Envoyer"
            android:layout_marginStart="8dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
